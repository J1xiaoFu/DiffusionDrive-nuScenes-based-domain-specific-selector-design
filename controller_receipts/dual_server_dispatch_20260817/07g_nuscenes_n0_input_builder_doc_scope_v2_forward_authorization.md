# 07G nuScenes N0 input-builder documentation-scope v2 authorization

Status: **AUTHORIZED — FORWARD-ONLY DOCUMENTATION CORRECTION**

Create one separate branch/worktree from exact accepted source commit
`2734b602e6ed92b6c1111bb36b4a9c02eacd471a`. Produce exactly one non-merge child commit changing
only `paper_iterations/P001_scheduler_session_contract/NUSCENES_N0_INPUT_BUILDER_SOURCE_V1.md`.
Preserve the accepted source-v1 branch and every RC, research, recovery, controller and public ref.

Correct exactly two scope statements:

1. Source-v1 accepts one explicitly addressed extracted metadata directory. Archive input is not
   implemented or accepted.
2. Real mode validates the metadata graph required to derive explicit sensor, map and CAN path
   expectations. Complete all-table closure remains the later frozen script-60 N0 gate. Synthetic
   script-60 replay proves interface compatibility only.

All library, CLI, test and configuration bytes must remain identical to the accepted base. Return the
exact child commit identity, clean status, documentation diff/hash, proof that source/test hashes did
not change, focused/adjacent synthetic CPU-test receipts and `git show --check`.

This authorization does not permit real data access, a real input specification, N0 evidence,
session membership, archive support, source/config/test changes, paper-claim promotion, publication,
transfer or 06G dispatch. Cache, model, T0, CUDA/GPU, training, evaluation and final-test access
remain stopped.
