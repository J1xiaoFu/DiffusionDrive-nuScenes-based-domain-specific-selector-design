# 07G nuScenes N0 input-builder source-v1 authorization

Status: **AUTHORIZED — FORWARD-ONLY SOURCE AND SYNTHETIC CPU FIXTURES**

Create a separate branch and worktree from exact accepted N0 provenance source commit
`ac38c9ffca9d4a428cee97606a92f93fb5ff8d82`. Produce exactly one non-merge commit changing only the
four paths listed in the JSON authorization. Preserve every accepted, RC, research, recovery,
controller and public ref.

The candidate must deterministically build the explicit, content-addressed input specification
consumed by frozen script 60. Every table, split source, devkit source and registered file must be
explicit and observed; discovery of roots or recursive inventory of shared sensor trees is forbidden.
Registered paths must be derived only from synthetic trainval-reachable tokens. An asserted release
label, pathname or unverified sidecar is not upstream provenance.

This phase may use only synthetic fixtures. It may not read or hash the real nuScenes/NAVSIM
installation, create a real input specification, generate an N0 receipt or manifest, import a model,
or use cache, T0, CUDA/GPU, training, evaluation or final-test data. Publication, push and 06G
dispatch remain controller-owned and stopped.

Return the exact branch, commit, parent, tree, clean status, changed-path and per-path SHA256
inventories, compile/static checks, focused and adjacent CPU test receipts, adversarial fixture
coverage and an explicit no-real-data/no-accelerator attestation.
