# P000 User Audit Request (Non-blocking)

The internal reviewer scores the current frozen program `3/10` and rejects it as a paper because no
official result exists and the OPD/LwF scheduler comparison is confounded. The automatic research loop
has already accepted those findings and moved to P001; user feedback is recorded separately and does
not block or silently alter P001.

Useful optional feedback concerns three scope choices:

1. whether Failure-Patch-CL should be presented as a constructed benchmark curriculum or whether a
   real deployment-arrival source can be supplied;
2. whether the pristine official experiment should use every eligible official token or a declared,
   provenance-complete session-atomic subset;
3. whether, after a null Seed-0 comparison, the preferred fallback contribution is a rigorous native
   benchmark/negative mechanism result rather than further method search.
